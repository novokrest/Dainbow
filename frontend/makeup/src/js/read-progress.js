var lodash = require('lodash');

class ReadProgress {

    static from(readActivities) {
        return 
    }

    constructor(readPagesCount) {
        this.readPagesCount = readPagesCount;
    }

    get readPagesCount() {
        return this.readPagesCount;
    }
}